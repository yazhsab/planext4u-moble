name: planext4u_storage
description: Encrypted cache and bounded offline command foundation for Planext4u.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.1
  flutter: ">=3.41.0"

dependencies:
  cryptography: ^2.9.0
  cryptography_flutter: ^2.3.4
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  path_provider: ^2.1.6
  planext4u_api_client: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
