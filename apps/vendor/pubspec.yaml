name: planext4u_vendor
description: Planext4u vendor application.
publish_to: 'none'
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  planext4u_config: ^0.1.0
  planext4u_core: ^0.1.0
  planext4u_design_system: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
